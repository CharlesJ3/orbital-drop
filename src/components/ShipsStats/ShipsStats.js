import React from 'react';
import './ShipsStats.scss';

function ShipsStats({}) {
	return (
		<div className="ships-stats">
			<span className="ships-stats__title"> Ship Name </span>
		</div>
	);
}

export default ShipsStats;
